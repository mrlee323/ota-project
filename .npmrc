# 앱이 워크스페이스 루트에 있다 (packages/* 는 디자인시스템뿐).
# 루트에 의존성을 추가하는 게 정상이므로 경고를 끈다.
ignore-workspace-root-check=true

# Node 22 미만이면 즉시 실패시킨다 — 20 에서는 require(ESM) 미지원으로
# 테스트가 31개 파일 전부 깨지는데 원인이 안 보인다.
engine-strict=true
